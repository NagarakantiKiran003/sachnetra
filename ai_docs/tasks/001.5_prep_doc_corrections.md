# Task 001.5 — Prep Doc Corrections
*SachNetra Adapt Sprint*

**Depends on**: Task 001 must be complete
**Estimated time**: 1 hour
**Prep doc**: All 8 docs in `ai_docs/prep/` (audit complete, corrections defined)

---

## Context — Current State

All 8 prep docs were written before studying the live WorldMonitor codebase.
An audit found **10 discrepancies** where prep doc examples would cause TypeScript errors or incorrect implementation during Tasks 002–007.

Key files that were cross-referenced:
- `src/types/index.ts` — `Feed` interface (lines 66–74), `MapLayers` interface (lines 571–635)
- `src/config/feeds.ts` — `SOURCE_TIERS` map, `SOURCE_PROPAGANDA_RISK` map, `getSourceTier()` function
- `src/config/variants/tech.ts` — established pattern for FEEDS, DEFAULT_PANELS, DEFAULT_MAP_LAYERS
- `src/config/variants/india.ts` — current state after Task 001 (empty FEEDS, minimal panels)

---

## What This Task Does

1. Corrects `04_data_sources.md` — fixes interface name, removes non-existent fields from feed examples, fixes type mismatches, adds `SOURCE_TIERS` entries section.
2. Corrects `03_system_design.md` — fixes `DEFAULT_PANELS` format, fixes `MapLayers` key names, resolves ais-relay.cjs contradiction.
3. Corrects `06_map_layers.md` — fixes `MapLayers` key names, adds note about `indiaStates` not existing yet.
4. Corrects `05_ai_prompt_spec.md` — adds warning about cache key format needing verification.

---

## Files To Open Before Starting

```
ai_docs/prep/04_data_sources.md    — most corrections (6 issues)
ai_docs/prep/03_system_design.md   — 3 issues to fix
ai_docs/prep/06_map_layers.md      — 2 issues to fix
ai_docs/prep/05_ai_prompt_spec.md  — 1 issue to fix
src/types/index.ts                 — reference: actual Feed interface (lines 66–74)
src/config/feeds.ts                — reference: SOURCE_TIERS map pattern
```

---

## Pattern To Follow

These are documentation files, not TypeScript. The code examples inside them must match the actual codebase interfaces. When correcting, quote the real interface and annotate where the tier/category data actually lives in the codebase.

---

## Implementation

### Phase 1: Fix `04_data_sources.md`
**Goal**: All feed examples compile against the real `Feed` interface. Tier info is documented to go in `SOURCE_TIERS` map, not on the feed objects.

- [ ] **Step 1.1** — Fix interface reference (line 19)
  - File: `ai_docs/prep/04_data_sources.md`
  - Change: `Format matches FeedConfig interface in src/config/feeds.ts`
  - To: `Format matches Feed interface in src/types/index.ts`

- [ ] **Step 1.2** — Rewrite all feed code blocks (lines 23–70)
  - Remove `category` and `tier` fields from every feed entry
  - Keep only: `name`, `url`, `region`, `propagandaRisk`, `stateAffiliated`, `lang`
  - Add comment above each tier section explaining that tier goes in `SOURCE_TIERS` map in `src/config/feeds.ts`
  - Organize feeds by category using section headers (the category IS the Record key, not a field)

  **Before:**
  ```typescript
  { name: 'NDTV', url: '...', category: 'Politics', tier: 1, region: 'national' },
  ```

  **After:**
  ```typescript
  // Category key: 'politics' — Tier 1 entries go in SOURCE_TIERS map in src/config/feeds.ts
  { name: 'NDTV', url: 'https://feeds.feedburner.com/ndtvnews-top-stories', region: 'national' },
  ```

- [ ] **Step 1.3** — Fix `propagandaRisk` type (line 52)
  - Change: `propagandaRisk: 0.1`
  - To: `propagandaRisk: 'low'`

- [ ] **Step 1.4** — Fix `stateAffiliated` type (lines 67, 69)
  - Change: `stateAffiliated: true`
  - To: `stateAffiliated: 'India'`

### Phase 2: Fix `03_system_design.md`
**Goal**: Code examples in system design match real codebase patterns.

- [ ] **Step 2.1** — Fix `DEFAULT_PANELS` format (lines 121–127)
  - File: `ai_docs/prep/03_system_design.md`
  - Change array format to `Record<string, PanelConfig>` format

  **Before:**
  ```typescript
  export const DEFAULT_PANELS = [
    'liveNews', 'insights', 'map',
  ]
  ```

  **After:**
  ```typescript
  export const DEFAULT_PANELS: Record<string, PanelConfig> = {
    'live-news': { name: 'India News', enabled: true, priority: 1 },
  };
  ```

- [ ] **Step 2.2** — Fix `MapLayers` key names (lines 92–115)
  - Change `earthquakes: true` to `natural: true`
  - Remove `vessels: false` (key doesn't exist; AIS tracking uses the key `ais`)
  - Add brief comment: `// "natural" covers earthquakes; "ais" covers vessels`

- [ ] **Step 2.3** — Fix ais-relay.cjs contradiction (line 363)
  - Change: `Railway relay scripts (scripts/ais-relay.cjs)`
  - To: `Railway relay infrastructure (scripts/ais-relay.cjs — don't change relay logic, but allowlist updates are fine per Task 002)`

### Phase 3: Fix `06_map_layers.md`
**Goal**: Layer key names match `MapLayers` interface.

- [ ] **Step 3.1** — Fix layer key names (lines 99–114, 119–130)
  - File: `ai_docs/prep/06_map_layers.md`
  - Change `earthquakes` → `natural` (with note: "key is `natural`, covers earthquakes + other natural events")
  - Change `vessels` → `ais` (with note: "key is `ais`, covers AIS ship tracking")

- [ ] **Step 3.2** — Add note about `indiaStates` (lines 135–140)
  - Add warning: `indiaStates` does NOT exist in the `MapLayers` interface yet. Task 006 must add it to `src/types/index.ts` before using it.

### Phase 4: Fix `05_ai_prompt_spec.md`
**Goal**: Cache key format flagged for verification.

- [ ] **Step 4.1** — Add verification warning (after line 129)
  - File: `ai_docs/prep/05_ai_prompt_spec.md`
  - Add a warning note after the cache key section:
    ```
    ⚠️ The `:india:` segment in the cache key above is aspirational.
    Verify the actual cache key format from groq-summarize.js
    before implementing. Current summaries are shared across variants.
    ```

---

## Read vs Write

**READ for reference (always allowed):**
- `src/types/index.ts` — verify Feed interface, MapLayers interface
- `src/config/feeds.ts` — verify SOURCE_TIERS, SOURCE_PROPAGANDA_RISK patterns
- `src/config/variants/tech.ts` — verify FEEDS/PANELS/MAP_LAYERS patterns

**WRITE only to files explicitly listed in this task:**
- `ai_docs/prep/04_data_sources.md` — feed examples + new sections (Phase 1)
- `ai_docs/prep/03_system_design.md` — panels format + map keys + contradiction (Phase 2)
- `ai_docs/prep/06_map_layers.md` — layer key names + indiaStates note (Phase 3)
- `ai_docs/prep/05_ai_prompt_spec.md` — cache key warning (Phase 4)

**Never write to:**
- `src/config/variants/full.ts` — sacred
- `src/config/variants/tech.ts` — sacred
- `src/config/variants/finance.ts` — sacred
- Any source code files — this task is documentation-only

---

## Verify

No `npm run typecheck` needed — this task modifies only markdown docs.

Verification is manual:
- [ ] All TypeScript code examples in corrected docs use only fields that exist on the `Feed` interface
- [ ] All `MapLayers` key references use actual interface keys (`natural`, `ais`, not `earthquakes`, `vessels`)
- [ ] `DEFAULT_PANELS` examples use `Record<string, PanelConfig>` format, not array
- [ ] `propagandaRisk` examples use `'low' | 'medium' | 'high'`, not numbers
- [ ] `stateAffiliated` examples use country name strings, not booleans
- [ ] No references to `FeedConfig` (doesn't exist — correct name is `Feed`)
- [ ] `NDTV`, `The Hindu`, etc. have `SOURCE_TIERS` entries documented for `src/config/feeds.ts`

---

## Completion Log

- [x] Phase 1 complete — `04_data_sources.md` corrected — 2026-03-19T14:55:00
- [x] Phase 2 complete — `03_system_design.md` corrected — 2026-03-19T14:58:00
- [x] Phase 3 complete — `06_map_layers.md` corrected — 2026-03-19T15:00:00
- [x] Phase 4 complete — `05_ai_prompt_spec.md` corrected — 2026-03-19T15:02:00
- [x] Manual verification — all code examples match real interfaces — 2026-03-19T15:10:00
- [x] **TASK 001.5 COMPLETE** ✅
